<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".profileFragment">

    <!-- Header -->
    <TextView
        android:id="@+id/profileHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:text="Sessions"
        android:textSize="24sp"
        android:textStyle="bold" />

    <!-- RecyclerView showing saved sessions -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewSessions"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="64dp"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:clipToPadding="false" />

    <ProgressBar
        android:id="@+id/progressLoadingSessions"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:visibility="gone" />

    <!-- Empty state shown when no sessions saved -->
    <TextView
        android:id="@+id/textNoSessions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="No saved sessions"
        android:textSize="16sp"
        android:visibility="gone"
        android:textColor="@android:color/darker_gray" />

    <!-- Save Session button at the top of the screen -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:orientation="horizontal">
        
        <Button
            android:id="@+id/btnSaveSession"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Save Current Pad Settings"
            android:drawableStart="@android:drawable/ic_menu_save"
            android:padding="12dp"
            android:backgroundTint="@color/purple_500"
            android:textColor="@android:color/white" />
            
        <ImageButton
            android:id="@+id/btnRefresh"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:src="@android:drawable/ic_popup_sync"
            android:backgroundTint="@color/purple_500"
            android:contentDescription="Refresh pad data" />
    </LinearLayout>

</FrameLayout>